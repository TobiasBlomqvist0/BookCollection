<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Update</title>
</head>
<body>

    <div style="width: 100%;display: flex; flex-direction: column; justify-content: center; align-items: center;">
        <% books.forEach(book => { %>
        <div style="display: flex; flex-direction: column; align-items: center; border: black solid 1px; border-bottom-left-radius: 12px; border-bottom-right-radius: 12px; padding: 0 2rem 2rem 2rem;">       
            <h1 style="border: black solid 1px; padding: 1rem; border-bottom-left-radius: 12px; border-bottom-right-radius: 12px;">Update book</h1>
            <h2 style="margin: 0.25rem;"><%= book.name %> </h2>
            <h3 style="margin: 0.25rem;"><%= book.author %> </h3>
            <img src="<%=book.img%>" alt="/book" style="width: 200px; height: 200px;">
        </div>
        <form action="" method="post" style="margin-top: 1rem;">
            <label for="name">Name</label>
            <input type="text" name="name" placeholder="<%= book.name %>" required>
            <label for="author">Author</label>
            <input type="text" name="author" placeholder="<%= book.author %>" required>
            <button>Update</button>
        </form> 
        <% }) %>
    </div>
    


</body>
</html>